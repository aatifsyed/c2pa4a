import { PublicKey } from "o1js";
export default function <T extends new (...args: any[]) => {
    raw: PublicKey | string;
    normalized: PublicKey;
}>(Base: T): {
    new (...args: any[]): {
        rawValue(): PublicKey | string;
        normalizedValue(): PublicKey;
        toZKValue(): PublicKey;
        raw: PublicKey | string;
        normalized: PublicKey;
    };
} & T;
