import { ZKLongitude } from "../../api/models/ZKLongitude";
import { ZKLatitude } from "../../api/models/ZKLatitude";
/**
 * Client for interacting with the HTTP Oracle service.
 */
export default class OracleClient {
    private oracleEndpoint;
    constructor(oracleEndpoint: string);
    fetchSignatureAndPublicKey(latitude: number | ZKLatitude, longitude: number | ZKLongitude): Promise<{
        signature: string;
        publicKey: string;
    }>;
}
