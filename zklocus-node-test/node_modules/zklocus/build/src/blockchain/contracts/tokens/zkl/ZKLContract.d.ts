import { SmartContract, State, DeployArgs, UInt64, PublicKey, Signature } from 'o1js';
/**
 * Token contract for the $ZKL token - the ecosystem token for zkLocus.
 *
 * The token is being developed iteratively, including it's interface.
 *
 */
export declare class ZKLContract extends SmartContract {
    SUPPLY_CAP: UInt64;
    circulatingSupply: State<UInt64>;
    deploy(args: DeployArgs): void;
    init(): void;
    mint(receiverAddress: PublicKey, amount: UInt64, adminSignature: Signature): void;
}
